
target-y = \
	rideturkey-gpxcompare${target_extension}

rideturkey-gpxcompare${target_extension}_files-y = \
	main.cpp

rideturkey-gpxcompare${target_extension}_includes-y += \
	${RIDETURKEY_GPXCOMPARE_3RDPARTY_INSTALL_PATH}/usr/local/include

dist.dir        = ../dist
dist.bin-y      = rideturkey-gpxcompare${target_extension}

include ${RIDETURKEY_GPXCOMPARE_LIBMAKEFILE_PATH}/Makefile.lib
